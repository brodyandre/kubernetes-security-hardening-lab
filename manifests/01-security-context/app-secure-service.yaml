apiVersion: v1
kind: Service
metadata:
  name: security-lab-app
  namespace: k8s-security-lab
  labels:
    app: security-lab-app
spec:
  type: ClusterIP
  selector:
    app: security-lab-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
